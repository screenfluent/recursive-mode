interface:
  display_name: "Recursive Spec"
  short_description: "Author requirements for Recursive runs and deliveries"
  default_prompt: "Use $recursive-spec to author or amend repo-aware requirements from the available context while preserving approval gates and native Recursive artifacts."
